# Class_Signup_Bot
Python Selenium script to be run automatically to sign up to classes. Hosted in an Azure Function App in a Linux docker container, running chrome webdriver. Thhe function runs on a time trigger and is customized depending on when the class needs to be booked.
